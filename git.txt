
    #define MRC_NOT_SAFE_MOVE               1
    char not_safe_move(char* move){

        return 0;
    }


    #define MRC_NOT_LEGAL_MOVE          \
            + MRC_NOT_IN_BOARD*2        \
            + 3 + 6




git config --global credential.helper store
git config --global core.editor "nano"
// git config --global --edit
git config --global user.name "Your Name"
git config --global user.email "name@domain.example"

// create a new repositor in github
echo "# chess" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/sirnaser/chess.git
git push -u origin main

git status
git log
git clone URL
git init
git add FILES
git commit -a -m "MESSAGE"
git push -u REPOSITORY BRANCH
git bransh
git bransh BRANCH_NAME
git switch BRANCH
git pull REPOSITORY BRANCH
// git fetch && git merge
git revert COMMIT_HASH
git reset --hard COMMIT_HASH
// git push --force REPOSITORY BRANCH

// back to the last commit
git stash

// back to the before stash
git stash pop

git checkout BRANCH
git merge DIST_BRANCH SOURCE_BRANCH

git push -d <remote_name> <branchname>   # Delete remote
git branch -d <branchname>               # Delete local
